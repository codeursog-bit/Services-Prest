name: Deploy Melanie

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 188.245.96.208
          username: deploy
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/deploy/Services-Prest
            git pull
            npm install
            npm run build
            pm2 restart melanie --update-env
            pm2 save
